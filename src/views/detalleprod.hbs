<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
    <title>Detalle del producto</title>
</head>
<body class="bg-gray-100">

<div class="container mx-auto p-6 max-w-screen-lg mt-8">
  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
   
    <div class="md:col-span-2 bg-white p-8 rounded shadow">
      <div class="space-y-6">
        <h1 class="text-2xl font-bold text-gray-900">{{product.title}}</h1>

        <div class="space-y-3 mt-5">
          <p class="text-lg text-gray-700">Precio: ${{product.price}}</p>
          <p class="text-lg text-gray-700">Descripción: {{product.description}}</p>
          <p class="text-lg text-gray-700">Stock disponible: {{product.stock}}</p>
        </div>

        <form action="/api/v1/cart/{{user.idCart}}/product/{{product._id}}" method="post" class="space-y-4">
          <div class="flex items-center space-x-4">
            <label for="quantity" class="text-lg text-gray-700">Cantidad:</label>
            <input type="number" id="quantity" name="quantity" class="w-16 border rounded-md p-1">
            <button type="submit" class="py-2 px-4 rounded bg-blue-500 text-white hover:bg-blue-600 focus:ring focus:ring-blue-200">
              Agregar al carrito
            </button>
          </div>
        </form>

      </div>
    </div>
  </div>
</div>

</body>
</html>
