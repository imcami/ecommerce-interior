<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-200">
    <div class="container mx-auto p-5">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">

            <div class="bg-white p-5 rounded-lg shadow-lg">
                <h1 class="text-2xl mb-5 font-semibold">Mis Productos</h1>
                <form action='/api/v1/products/upload' method="post" enctype="multipart/form-data" class="space-y-4">
                  <ul>
                    <li>
                        <label for="name" class="block text-gray-700 font-semibold">Nombre</label>
                        <input type="text" name="name" id="name" class="w-full border-gray-300 rounded-lg shadow-sm focus:border-blue-500 focus:ring-blue-500">
                         
                        <label for="description" class="text-gray-700 font-semibold">Descripcion</label>
                        <input type="text" name="description" id="description" class="w-full border-gray-300 rounded-lg shadow-sm focus:border-blue-500 focus:ring-blue-500">
                        

                    </li>
                  </ul>

                    <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">
                        Subir Producto
                    </button>
                </form>

                <div class="mt-5">
                    {{#if products}}
                    <h2 class="text-xl mb-3 font-semibold">Productos Subidos:</h2>
                    <ul>
                        {{#each products}}
                        <li class="p-3 bg-gray-100 mb-2 rounded-lg flex justify-between items-center">
                            <div>
                                <img src="{{this.image}}" alt="{{this.name}}" class="w-10 h-10 rounded">
                            </div>
                            <div>
                                <span class="font-medium text-gray-700">{{this.name}}</span>
                            </div>
                            <div>
                                <span class="font-medium text-blue-600">${{this.price}}</span>
                            </div>
                        </li>
                        {{/each}}
                    </ul>
                    {{else}}
                    <p class="text-gray-500">No has subido ningún producto aún.</p>
                    {{/if}}
                </div>
            </div>

        </div>
    </div>
</body>
</html>
